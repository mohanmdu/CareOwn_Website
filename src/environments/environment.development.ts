export const environment = {
  production: false,
  // Matches hms-api's default local port (see hms-api/src/main/resources/application.properties).
  apiBaseUrl: 'http://localhost:8080',
  recaptchaEnabled: false,
  recaptchaSiteKey: ''
};
